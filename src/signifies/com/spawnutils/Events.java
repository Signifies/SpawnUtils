package signifies.com.spawnutils;

import org.bukkit.event.Listener;

public class Events implements Listener {

    private SpawnUtils instance;
    public Events(SpawnUtils spawnUtils) {
        instance = spawnUtils;
    }
}
